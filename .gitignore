# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
.env

# Output files (generated at runtime — not committed)
output/
signals.jsonl
monitor.log
pairs.json

# OS
.DS_Store
Thumbs.db

# Secrets (never commit)
*.pem
*.key
.env
.env.*

# Alerter: secrets + runtime state (never commit)
alert_config.json
alert_state.json
alert_signals.jsonl

# Catalog cache (runtime, never commit)
.cache/
