# Core HTTP client — required for all functionality
requests>=2.28.0

# Optional: Kalshi authenticated endpoints (order placement, balance, positions)
# Needed only if you set KALSHI_API_KEY + KALSHI_PRIVATE_KEY_PATH
# cryptography>=41.0.0

# Optional: Polymarket authenticated order placement
# Requires an Ethereum private key + L2 API credentials
# eth-account>=0.10.0
# py-clob-client-v2>=0.1.0    # EIP-712 order signing for CLOB
